
// Write a function that takes the hour as an input parameter. And it will convert
//  those hours to minutes and return minutes.

const changeToMinutes = (num) => {
  const minutes = num * 60;
  return console.log(minutes);
};

changeToMinutes(12);