// Create an array with the names of all the things you have learned in this lesson.
//  Then use a for loop to output all elements of that array.

const fruits = ["Apple", "Banana", "Orange", "Mango", "strawberry"];

const loopIteration = () => {
    for(var i = 0; i <= fruits.length; i++){
          console.log(fruits[i]);
    }
};

loopIteration();
