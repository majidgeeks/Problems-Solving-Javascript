// Calculate the interest. Just how to calculate. Will do with that thought.
// Now there is no need to worry whether interest is good or bad.
// Just try to do it from the angle of how to write it in code if there is a formula.

const calcuateInterest = (num) => {
  const rate = 5/100 ;
  const interest = num * rate;
  console.log("interest",interest);

};

const investment = 100000 
calcuateInterest(investment);